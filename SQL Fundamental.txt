SQL Fundamental

1. DDL (Data Definition Language)

CREATE DATABASE → bikin database

CREATE DATABASE db_test;


CREATE TABLE → bikin tabel baru

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL,
    password VARCHAR(255) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);


ALTER TABLE → ubah struktur tabel

ALTER TABLE users ADD phone VARCHAR(20);


DROP TABLE → hapus tabel

DROP TABLE users;

🔹 2. DML (Data Manipulation Language)

INSERT → tambah data

INSERT INTO users (username, email, password)
VALUES ('Gerry', 'gerry@mail.com', '12345');


UPDATE → ubah data

UPDATE users SET password = 'newpass123'
WHERE username = 'Gerry';


DELETE → hapus data

DELETE FROM users WHERE id = 1;

🔹 3. SELECT (Query Dasar)

SELECT semua kolom

SELECT * FROM users;


SELECT kolom tertentu

SELECT username, email FROM users;


WHERE (filter)

SELECT * FROM users WHERE username = 'Gerry';


LIKE (pencarian pola)

SELECT * FROM users WHERE email LIKE '%@mail.com';


ORDER BY (sorting)

SELECT * FROM users ORDER BY created_at DESC;


LIMIT (batasi jumlah data)

SELECT * FROM users LIMIT 5;

🔹 4. Aggregate Functions

COUNT() → hitung jumlah baris

SELECT COUNT(*) AS total_users FROM users;


SUM() → jumlah total

SELECT SUM(price) AS total_sales FROM orders;


AVG() → rata-rata

SELECT AVG(price) AS avg_price FROM orders;


MIN() / MAX() → nilai terkecil / terbesar

SELECT MIN(price), MAX(price) FROM orders;


GROUP BY → kelompokkan data

SELECT user_id, COUNT(*) AS total_orders
FROM orders
GROUP BY user_id;


HAVING → filter setelah GROUP BY

SELECT user_id, COUNT(*) AS total_orders
FROM orders
GROUP BY user_id
HAVING COUNT(*) > 3;

🔹 5. JOIN (Menggabungkan Tabel)

INNER JOIN → hanya data yang cocok

SELECT u.username, o.product_name
FROM users u
JOIN orders o ON u.id = o.user_id;


LEFT JOIN → semua data dari kiri (users) walau gak ada order

SELECT u.username, o.product_name
FROM users u
LEFT JOIN orders o ON u.id = o.user_id;

🔹 6. Subquery

Subquery dalam WHERE

SELECT * FROM users
WHERE id IN (SELECT user_id FROM orders);

🔹 7. Fungsi String & Date (sering dipakai)

UPPER() / LOWER() → ubah huruf

SELECT UPPER(username), LOWER(email) FROM users;


CONCAT() → gabung string

SELECT CONCAT(username, ' - ', email) AS user_info FROM users;


SUBSTRING() → ambil sebagian string

SELECT SUBSTRING(email, 1, 5) FROM users;


NOW() → waktu sekarang

SELECT NOW();


YEAR(), MONTH(), DAY() → ambil bagian tanggal

SELECT YEAR(created_at), MONTH(created_at) FROM users;